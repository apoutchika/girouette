version: '3.7'

services:
  proxy:
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: always
    networks:
      - proxy
      - traefik
      - webgateway
    ports:
      - 443:443
      - 80:80
    volumes:
      - './app:/app'
      - './data:/data'
      - '/var/run/docker.sock:/var/run/docker.sock'
    labels:
      proxy.domains: proxy.devel:8080

  whoami:
    image: emilevauge/whoami
    networks:
      - proxy
      - traefik
      - webgateway
    restart: always
    labels:
      proxy_domain: whoami.devel
      proxy_port: 80
      traefik.frontend.rule: 'Host:whoami.devel'
      traefik.port: 80

networks:
  traefik:
    external:
      name: traefik
  proxy:
    external:
      name: proxy
  webgateway:
    external:
      name: traefikforwebdev_webgateway
